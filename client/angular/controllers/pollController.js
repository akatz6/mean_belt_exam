app.controller('pollController', ['$scope', '$location', 'pollFactory',function($scope, $location, pollFactory){
	// index = function(){
	// 	welcomeFactory.get_name(function(returned_data){
	// 		$scope.name = returned_data.data.user
	// 		console.log($scope.name) 
	// 	})
	// }
	// index();
}]);